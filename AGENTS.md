## 代码检索

检索优先级：LSP -> rg -> 小范围读文件

**1. LSP（精确符号操作）**

处理代码符号时必须先用 LSP：
- `documentSymbol` -> 文件结构概览，替代读全文
- `goToDefinition` / `findReferences` -> 精确到行的符号定位
- `hover` -> 类型签名
- `getDiagnostics` -> 快速查看当前文件诊断

LSP 冷启动失败后等待 3 秒并重试一次。两次失败后才能回退到 `rg`，并说明具体错误。不要臆造当前会话里没有暴露的 LSP 工具。

**2. rg（文本搜索）**

用于路由字符串、界面文案、配置 key、错误消息、环境变量和非代码文件。

**3. git / 小范围读文件**

- `git` 查看状态、分支、提交和 diff
- 只精读前面工具定位到的行范围，不读全文
